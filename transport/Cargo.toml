[package]
name = "transport"
version = "0.1.0"
edition = "2024"

[dependencies]
async-trait = "0.1.89"
domain = { version = "0.1.0", path = "../domain" }
tokio = { version = "1.50.0", features = ["macros", "rt-multi-thread"] }
